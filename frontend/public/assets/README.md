# Public Assets Directory

Place static public assets here (e.g. `logo.png`, `logo.svg`).

Any file in `frontend/public/assets/` can be accessed in frontend components using the URL path `/assets/<filename>`.
